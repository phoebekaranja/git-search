import { TimeellapsedPipePipe } from './time-ellapsed-pipe.pipe';

describe('TimeellapsedPipePipe', () => {
  it('create an instance', () => {
    const pipe = new time-ellapsedPipePipe();
    expect(pipe).toBeTruthy();
  });
});
